-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 7df5f1c7d683dc56d521679b882f50d1d68dec40fc7f5dc2c15fd44bab264994
-- Directly compiled rules: SpaceCall, SpaceArg, Primary, CarrierLiftTupleExpr, LiftTupleExpr, PanicExpr, JsonExpr, JsonObjectFieldList, JsonObjectField, RecordExpr
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_SpaceCall = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("SpaceCall:root",
    matchSequence([
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("arg"),
          parseRule("SpaceArg", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_SpaceArg = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("Lambda", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Primary", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_Primary = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("Float", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Int", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("InterpolatedString", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("String", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Bool", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Void", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("Primary:root.alternatives.6",
        matchLabeled(Some("hole"),
          matchLiteral([63], "?", "Primary", source, offset)
        )
      )
    },
    (source, offset) => {
      parseRule("PanicExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("CarrierLiftTupleExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("LiftTupleExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("JsonExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("NamedRecordExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("RecordExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ParenSeq", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("TupleOrGroup", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Block", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("AnonymousMatchFn", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("Primary:root.alternatives.18",
        matchLabeled(Some("v"),
          parseRule("LongName", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_CarrierLiftTupleExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("CarrierLiftTupleExpr:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("carrier"),
          parseRule("LongName", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([124], "|", "CarrierLiftTupleExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("args"),
          parseRule("ArgList", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([124], "|", "CarrierLiftTupleExpr", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_LiftTupleExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LiftTupleExpr:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([124], "|", "LiftTupleExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("args"),
          parseRule("ArgList", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([124], "|", "LiftTupleExpr", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_PanicExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("PanicExpr:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([80, 97, 110, 105, 99], "Panic", "PanicExpr", source, offset)
      },
      (source, offset) => {
        matchNegativeLookahead(
          parseRule("IdentPart", source, offset, diagnose, false)
        , offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([40], "(", "PanicExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("message"),
          parseRule("Expr", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "PanicExpr", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_JsonExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("JsonExpr:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([74, 83, 79, 78], "JSON", "JsonExpr", source, offset)
          },
          (source, offset) => {
            matchNegativeLookahead(
              parseRule("IdentPart", source, offset, diagnose, false)
            , offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableOpeningBrace("JsonExpr:open-brace", recover, source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("fields"),
              matchOptional(
                parseRule("JsonObjectFieldList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableLiteral([125], "}", "JsonExpr:close-brace", recover, source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsonExpr:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([74, 83, 79, 78], "JSON", "JsonExpr", source, offset)
          },
          (source, offset) => {
            matchNegativeLookahead(
              parseRule("IdentPart", source, offset, diagnose, false)
            , offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([91], "[", "JsonExpr", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("items"),
              matchOptional(
                parseRule("ArgList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "JsonExpr", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_JsonObjectFieldList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("JsonObjectFieldList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("JsonObjectField", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("JsonObjectFieldList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "JsonObjectFieldList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("JsonObjectField", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "JsonObjectFieldList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_JsonObjectField = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("JsonObjectField:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("key"),
              parseRule("FieldName", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([58], ":", "JsonObjectField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("value"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsonObjectField:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("key"),
              parseRule("String", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([58], ":", "JsonObjectField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("value"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_RecordExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordExpr:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([46], ".", "RecordExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("RecordExpr:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordExprFieldList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "RecordExpr:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};
