-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: a5698108ba389db91ff03ab8795226ccc23344f605036261786d594354f4f248
-- Directly compiled rules: ListParamPattern, RecordPattern, RecordPatternFieldList, RecordPatternField, RecordLetPattern, RecordLetPatternFieldList, RecordLetPatternField, RecordParamPattern
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_ListParamPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ListParamPattern:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListParamPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListParamPattern:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([46, 46], "..", "ListParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              parseRule("ParamPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListParamPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListParamPattern:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("ParamPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchRepeated((source, offset) => {
                matchSyntaxAction("ListParamPattern:root.alternatives.2.expression.elements.3.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListParamPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("p"),
                        parseRule("ParamPattern", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              }, diagnose, source, offset)
            )
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              matchOptional(
                matchSyntaxAction("ListParamPattern:root.alternatives.2.expression.elements.4.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListParamPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([46, 46], "..", "ListParamPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("t"),
                        parseRule("ParamPattern", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchOptional(
              matchLiteral([44], ",", "ListParamPattern", source, offset)
            , diagnose, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListParamPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_RecordPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordPattern:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([46], ".", "RecordPattern", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("RecordPattern:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordPatternFieldList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "RecordPattern:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordPatternFieldList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordPatternFieldList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("RecordPatternField", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("RecordPatternFieldList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "RecordPatternFieldList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("RecordPatternField", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "RecordPatternFieldList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordPatternField = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("RecordPatternField:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("FieldName", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61], "=", "RecordPatternField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("Pattern", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("RecordPatternField:root.alternatives.1",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_RecordLetPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordLetPattern:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([46], ".", "RecordLetPattern", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("RecordLetPattern:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordLetPatternFieldList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "RecordLetPattern:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordLetPatternFieldList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordLetPatternFieldList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("RecordLetPatternField", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("RecordLetPatternFieldList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "RecordLetPatternFieldList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("RecordLetPatternField", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "RecordLetPatternFieldList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordLetPatternField = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("RecordLetPatternField:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("FieldName", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61], "=", "RecordLetPatternField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("RecordLetPatternField:root.alternatives.1",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_RecordParamPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordParamPattern:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([46], ".", "RecordParamPattern", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("RecordParamPattern:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordParamPatternFieldList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "RecordParamPattern:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};
