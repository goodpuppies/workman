-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: e229671a9f698fe9a83cde98c7e5757ce91ac2fd2484ca7881c86eacd0dc6b52
-- Directly compiled rules: RecordParamPatternFieldList, RecordParamPatternField, TypeAnn, FnType, FunctionType, FnTypeDomain, GroupedFunctionType, TypeAtom, TypeList, TypeParams
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_RecordParamPatternFieldList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordParamPatternFieldList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("RecordParamPatternField", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("RecordParamPatternFieldList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "RecordParamPatternFieldList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("RecordParamPatternField", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "RecordParamPatternFieldList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordParamPatternField = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("RecordParamPatternField:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("FieldName", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61], "=", "RecordParamPatternField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("ParamPattern", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("RecordParamPatternField:root.alternatives.1",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_TypeAnn = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("TypeAnn:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([58], ":", "TypeAnn", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("t"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_FnType = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("FunctionType", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("TypeAtom", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_FunctionType = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("FunctionType:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("ps"),
          parseRule("FnTypeDomain", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([45, 62], "->", "FunctionType", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("r"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_FnTypeDomain = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("FnTypeDomain:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "FnTypeDomain", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("FnType", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "FnTypeDomain", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("TypeList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "FnTypeDomain", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("FnTypeDomain:root.alternatives.1",
        matchLabeled(Some("t"),
          parseRule("TypeAtom", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_GroupedFunctionType = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("GroupedFunctionType:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([40], "(", "GroupedFunctionType", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("t"),
          parseRule("FunctionType", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "GroupedFunctionType", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_TypeAtom = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("TypeAtom:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "TypeAtom", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("FnType", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "TypeAtom", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("TypeList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "TypeAtom", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      parseRule("GroupedFunctionType", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("TypeAtom:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("args"),
              matchOptional(
                matchSyntaxAction("TypeAtom:root.alternatives.2.expression.elements.1.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      matchLiteral([60], "<", "TypeAtom", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("xs"),
                        parseRule("TypeList", source, offset, diagnose, false)
                      )
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([62], ">", "TypeAtom", source, offset)
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("TypeAtom:root.alternatives.3",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_TypeList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("TypeList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("TypeList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "TypeList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("t"),
                    parseRule("FnType", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "TypeList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_TypeParams = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("TypeParams:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([60], "<", "TypeParams", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("TypeParam", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("TypeParams:root.expression.elements.3.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "TypeParams", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("t"),
                    parseRule("TypeParam", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "TypeParams", source, offset)
        , diagnose, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([62], ">", "TypeParams", source, offset)
      },
    ], diagnose, source, offset)
  )
};
