-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 7df5f1c7d683dc56d521679b882f50d1d68dec40fc7f5dc2c15fd44bab264994
-- Directly compiled rules: NamedRecordExpr, RecordExprFieldList, RecordExprField, ListExpr, TupleOrGroup, ParenSeq, Block, BlockSeqBody
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_NamedRecordExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("NamedRecordExpr:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("target"),
          matchSyntaxAction("NamedRecordExpr:root.expression.elements.0.expression",
            matchLabeled(Some("name"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          )
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([123], "{", "NamedRecordExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordExprFieldList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([125], "}", "NamedRecordExpr", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordExprFieldList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordExprFieldList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("RecordExprField", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("RecordExprFieldList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "RecordExprFieldList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("RecordExprField", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "RecordExprFieldList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordExprField = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("RecordExprField:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([46, 46], "..", "RecordExprField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("value"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("RecordExprField:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("FieldName", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61], "=", "RecordExprField", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("value"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("RecordExprField:root.alternatives.2",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_ListExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ListExpr:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListExpr", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListExpr", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListExpr:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListExpr", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([46, 46], "..", "ListExpr", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListExpr", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListExpr:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListExpr", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchRepeated((source, offset) => {
                matchSyntaxAction("ListExpr:root.alternatives.2.expression.elements.3.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListExpr", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("e"),
                        parseRule("Expr", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              }, diagnose, source, offset)
            )
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              matchOptional(
                matchSyntaxAction("ListExpr:root.alternatives.2.expression.elements.4.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListExpr", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([46, 46], "..", "ListExpr", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("t"),
                        parseRule("Expr", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchOptional(
              matchLiteral([44], ",", "ListExpr", source, offset)
            , diagnose, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListExpr", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_TupleOrGroup = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("TupleOrGroup:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "TupleOrGroup", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "TupleOrGroup", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("ArgList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "TupleOrGroup", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("TupleOrGroup:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "TupleOrGroup", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("e"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "TupleOrGroup", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_ParenSeq = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParenSeq:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([40], "(", "ParenSeq", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("seq"),
          parseRule("ParenSeqBody", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "ParenSeq", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_Block = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Block:root",
    matchSequence([
      (source, offset) => {
        matchRecoverableOpeningBrace("Block:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("seq"),
          parseRule("BlockSeqBody", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "Block:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_BlockSeqBody = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("BlockSeqBody:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("entries"),
          matchRepeated((source, offset) => {
            parseRule("BlockSeqItem", source, offset, diagnose, recover)
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("result"),
          matchOptional(
            parseRule("Expr", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};
