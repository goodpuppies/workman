-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 195621adf008708ca11f8121167021a542a7dfb2f34917ce6b46eaf29071514d
-- Directly compiled rules: ParamPattern, ParamListItems, ParamPatternList, PatternList, CtorPattern, CtorPatternList, ArgList
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_ParamPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ParamPattern:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "ParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("ParamPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("annotation"),
              parseRule("TypeAnn", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "ParamPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ParamPattern:root.alternatives.1",
        parseRule("Wildcard", source, offset, diagnose, recover)
      )
    },
    (source, offset) => {
      parseRule("RecordParamPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListParamPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("ParamPattern:root.alternatives.4",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "ParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("ParamPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "ParamPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("ParamPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "ParamPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ParamPattern:root.alternatives.5",
        matchLabeled(Some("v"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_ParamListItems = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParamListItems:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Param", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("ParamListItems:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "ParamListItems", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("Param", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "ParamListItems", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_ParamPatternList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParamPatternList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("ParamPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("ParamPatternList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "ParamPatternList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("ParamPattern", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "ParamPatternList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_PatternList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("PatternList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Pattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("PatternList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "PatternList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("Pattern", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "PatternList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_CtorPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("RecordPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("CtorPattern:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchNegativeLookahead(
              matchLiteral([86, 97, 114], "Var", "CtorPattern", source, offset)
            , offset)
          },
          (source, offset) => {
            matchLabeled(Some("c"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "CtorPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("args"),
              matchOptional(
                parseRule("CtorPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "CtorPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("CtorPattern:root.alternatives.3",
        matchSequence([
          (source, offset) => {
            matchNegativeLookahead(
              matchLiteral([86, 97, 114], "Var", "CtorPattern", source, offset)
            , offset)
          },
          (source, offset) => {
            matchLabeled(Some("c"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("CtorPattern:root.alternatives.4",
        matchLabeled(Some("v"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("CtorPattern:root.alternatives.5",
        matchSequence([
          (source, offset) => {
            matchNegativeLookahead(
              matchLiteral([86, 97, 114], "Var", "CtorPattern", source, offset)
            , offset)
          },
          (source, offset) => {
            matchLabeled(Some("p"),
              parseRule("Pattern", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_CtorPatternList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("CtorPatternList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("CtorPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("CtorPatternList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "CtorPatternList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("CtorPattern", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "CtorPatternList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_ArgList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ArgList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Expr", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("ArgList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "ArgList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("e"),
                    parseRule("Expr", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "ArgList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};
