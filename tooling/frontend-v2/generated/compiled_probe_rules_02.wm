-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: a5698108ba389db91ff03ab8795226ccc23344f605036261786d594354f4f248
-- Directly compiled rules: LetBinding, LetPattern, LetPatternList, LetCtorPattern, LetCtorPatternList, RecordDecl
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_LetBinding = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LetBinding:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("pattern"),
          parseRule("LetPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("ann"),
          matchOptional(
            parseRule("TypeAnn", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([61], "=", "LetBinding", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("value"),
          parseRule("Expr", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_LetPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("annotation"),
              parseRule("TypeAnn", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "LetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.1",
        parseRule("Wildcard", source, offset, diagnose, recover)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.2",
        matchLiteral([118, 111, 105, 100], "void", "LetPattern", source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.3",
        matchLabeled(Some("b"),
          parseRule("Bool", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.4",
        matchLabeled(Some("i"),
          parseRule("Int", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.5",
        matchLabeled(Some("s"),
          parseRule("String", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      parseRule("RecordLetPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListLetPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.8",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("LetPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "LetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.9",
        matchSequence([
          (source, offset) => {
            matchLiteral([86, 97, 114], "Var", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("Name", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "LetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.10",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("c"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "LetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("args"),
              matchOptional(
                parseRule("LetCtorPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "LetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.11",
        matchLabeled(Some("c"),
          parseRule("LongConstructor", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetPattern:root.alternatives.12",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_LetPatternList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LetPatternList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("LetPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LetPatternList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "LetPatternList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("LetPattern", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "LetPatternList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_LetCtorPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("LetCtorPattern:root.alternatives.0",
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("LetCtorPattern:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchNegativeLookahead(
              matchLiteral([86, 97, 114], "Var", "LetCtorPattern", source, offset)
            , offset)
          },
          (source, offset) => {
            matchLabeled(Some("p"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_LetCtorPatternList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LetCtorPatternList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("LetCtorPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LetCtorPatternList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "LetCtorPatternList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("p"),
                    parseRule("LetCtorPattern", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "LetCtorPatternList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordDecl:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([114, 101, 99, 111, 114, 100], "record", "RecordDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("Constructor", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("params"),
          matchOptional(
            parseRule("TypeParams", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([61], "=", "RecordDecl", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("RecordDecl:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("fields"),
          matchOptional(
            parseRule("RecordFieldDeclList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "RecordDecl:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};
