-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 195621adf008708ca11f8121167021a542a7dfb2f34917ce6b46eaf29071514d
-- Directly compiled rules: MatchArmList, MatchArm, Lambda, LambdaReturnAnnotation, LambdaBody, LambdaBlock, Directive, Or, And, Equality
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_MatchArmList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MatchArmList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("MatchArm", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("MatchArmList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "MatchArmList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("a"),
                    parseRule("MatchArm", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "MatchArmList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_MatchArm = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MatchArm:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("pattern"),
          parseRule("Pattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([61, 62], "=>", "MatchArm", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("body"),
          parseRule("Block", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Lambda = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("Lambda:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("params"),
              parseRule("ParamList", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("returnAnnotation"),
              matchOptional(
                parseRule("LambdaReturnAnnotation", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61, 62], "=>", "Lambda", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("parsed"),
              parseRule("LambdaBody", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("trailingReturnAnnotation"),
              matchOptional(
                parseRule("LambdaReturnAnnotation", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("Lambda:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("returnAnnotation"),
              matchOptional(
                parseRule("LambdaReturnAnnotation", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([61, 62], "=>", "Lambda", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("parsed"),
              parseRule("LambdaBody", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("trailingReturnAnnotation"),
              matchOptional(
                parseRule("LambdaReturnAnnotation", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_LambdaReturnAnnotation = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LambdaReturnAnnotation:root",
    matchSequence([
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([58], ":", "LambdaReturnAnnotation", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("type"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_LambdaBody = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("LambdaBlock", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("LambdaBody:root.alternatives.1",
        matchLabeled(Some("body"),
          parseRule("Lambda", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("LambdaBody:root.alternatives.2",
        matchLabeled(Some("body"),
          parseRule("ParenSeq", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_LambdaBlock = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LambdaBlock:root",
    matchSequence([
      (source, offset) => {
        matchRecoverableOpeningBrace("LambdaBlock:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("directives"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LambdaBlock:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  matchLabeled(Some("directive"),
                    parseRule("Directive", source, offset, diagnose, recover)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, recover)
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("seq"),
          parseRule("BlockSeqBody", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "LambdaBlock:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_Directive = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Directive:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([64], "@", "Directive", source, offset)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        parseRule("SemiToken", source, offset, diagnose, recover)
      },
    ], diagnose, source, offset)
  )
};

let rule_Or = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Or:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("And", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Or:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchLiteral([124, 124], "||", "Or", source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("And", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_And = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("And:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Equality", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("And:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchLiteral([38, 38], "&&", "And", source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Equality", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Equality = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Equality:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Compare", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Equality:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchChoice([
                      (source, offset) => {
                        matchLiteral([61, 61], "==", "Equality", source, offset)
                      },
                      (source, offset) => {
                        matchLiteral([33, 61], "!=", "Equality", source, offset)
                      },
                    ], diagnose, source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Compare", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};
