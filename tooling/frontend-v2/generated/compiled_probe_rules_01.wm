-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 195621adf008708ca11f8121167021a542a7dfb2f34917ce6b46eaf29071514d
-- Directly compiled rules: JsTarget, JsImportClause, JsImportClauseBody, JsImportSpecList, JsImportSpec, LetDecl
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_JsTarget = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("JsTarget:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([106, 115, 46, 103, 108, 111, 98, 97, 108], "js.global", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("path"),
              parseRule("String", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "JsTarget", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsTarget:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([106, 115, 46, 103, 108, 111, 98, 97, 108], "js.global", "JsTarget", source, offset)
          },
          (source, offset) => {
            matchNegativeLookahead(
              parseRule("IdentPart", source, offset, diagnose, false)
            , offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsTarget:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLiteral([106, 115, 46, 109, 101, 116, 97], "js.meta", "JsTarget", source, offset)
          },
          (source, offset) => {
            matchNegativeLookahead(
              parseRule("IdentPart", source, offset, diagnose, false)
            , offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsTarget:root.alternatives.3",
        matchSequence([
          (source, offset) => {
            matchLiteral([106, 115, 46, 109, 111, 100, 117, 108, 101], "js.module", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("specifier"),
              parseRule("String", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "JsTarget", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsTarget:root.alternatives.4",
        matchSequence([
          (source, offset) => {
            matchLiteral([106, 115, 46, 119, 111, 114, 107, 101, 114], "js.worker", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "JsTarget", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("specifier"),
              parseRule("String", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "JsTarget", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_JsImportClause = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("JsImportClause:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("unsafe"),
          matchOptional(
            matchSequence([
              (source, offset) => {
                matchLiteral([117, 110, 115, 97, 102, 101], "unsafe", "JsImportClause", source, offset)
              },
              (source, offset) => {
                parseRule("__", source, offset, diagnose, false)
              },
            ], diagnose, source, offset)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("clause"),
          parseRule("JsImportClauseBody", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_JsImportClauseBody = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("JsImportClauseBody:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([42], "*", "JsImportClauseBody", source, offset)
          },
          (source, offset) => {
            parseRule("__", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([97, 115], "as", "JsImportClauseBody", source, offset)
          },
          (source, offset) => {
            parseRule("__", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("alias"),
              parseRule("Name", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("JsImportClauseBody:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchRecoverableOpeningBrace("JsImportClauseBody:open-brace", recover, source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("specs"),
              matchOptional(
                parseRule("JsImportSpecList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableLiteral([125], "}", "JsImportClauseBody:close-brace", recover, source, offset)
          },
          (source, offset) => {
            matchLabeled(Some("alias"),
              matchOptional(
                matchSyntaxAction("JsImportClauseBody:root.alternatives.1.expression.elements.5.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("__", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([97, 115], "as", "JsImportClauseBody", source, offset)
                    },
                    (source, offset) => {
                      parseRule("__", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("a"),
                        parseRule("Name", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_JsImportSpecList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("JsImportSpecList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("JsImportSpec", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("JsImportSpecList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "JsImportSpecList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("s"),
                    parseRule("JsImportSpec", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "JsImportSpecList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_JsImportSpec = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("JsImportSpec:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("MemberName", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("alias"),
          matchOptional(
            matchSyntaxAction("JsImportSpec:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([97, 115], "as", "JsImportSpec", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("a"),
                    parseRule("Name", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("type"),
          matchOptional(
            matchSyntaxAction("JsImportSpec:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([58], ":", "JsImportSpec", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("t"),
                    parseRule("FnType", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_LetDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LetDecl:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([108, 101, 116], "let", "LetDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("rec"),
          matchOptional(
            matchSequence([
              (source, offset) => {
                matchLiteral([114, 101, 99], "rec", "LetDecl", source, offset)
              },
              (source, offset) => {
                parseRule("__", source, offset, diagnose, false)
              },
            ], diagnose, source, offset)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("LetBinding", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LetDecl:root.expression.elements.4.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([97, 110, 100], "and", "LetDecl", source, offset)
                },
                (source, offset) => {
                  parseRule("__", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("b"),
                    parseRule("LetBinding", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};
