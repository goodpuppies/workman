-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 195621adf008708ca11f8121167021a542a7dfb2f34917ce6b46eaf29071514d
-- Directly compiled rules: BlockSeqItem, BlockDecl, ParenSeqBody, ParenSeqItem, SemiToken, Pattern, PatternCore, ParamList, Param
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_BlockSeqItem = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("BlockSeqItem:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("d"),
              parseRule("BlockDecl", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            parseRule("SemiToken", source, offset, diagnose, recover)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("BlockSeqItem:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("e"),
              parseRule("Expr", source, offset, diagnose, false)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, false)
          },
          (source, offset) => {
            matchLabeled(Some("semi"),
              parseRule("SemiToken", source, offset, diagnose, false)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, false)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_BlockDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("RecordDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("TypeDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("LetDecl", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_ParenSeqBody = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParenSeqBody:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("entries"),
          matchOneOrMore((source, offset) => {
            parseRule("ParenSeqItem", source, offset, diagnose, false)
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("result"),
          matchOptional(
            parseRule("Expr", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_ParenSeqItem = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParenSeqItem:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("e"),
          parseRule("Expr", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("semi"),
          parseRule("SemiToken", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
    ], diagnose, source, offset)
  )
};

let rule_SemiToken = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("SemiToken:root",
    matchRecoverableLiteral([59], ";", "SemiToken:semicolon", recover, source, offset)
  )
};

let rule_Pattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Pattern:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("pattern"),
          parseRule("PatternCore", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("annotation"),
          matchOptional(
            matchSyntaxAction("Pattern:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("value"),
                    parseRule("TypeAnn", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_PatternCore = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.0",
        parseRule("Wildcard", source, offset, diagnose, recover)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.1",
        matchLiteral([118, 111, 105, 100], "void", "PatternCore", source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.2",
        matchLabeled(Some("b"),
          parseRule("Bool", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.3",
        matchLabeled(Some("i"),
          parseRule("Int", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.4",
        matchLabeled(Some("s"),
          parseRule("String", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      parseRule("RecordPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.7",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("Pattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([44], ",", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchOptional(
                parseRule("PatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "PatternCore", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.8",
        matchSequence([
          (source, offset) => {
            matchLiteral([40], "(", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("pattern"),
              parseRule("Pattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "PatternCore", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.9",
        matchSequence([
          (source, offset) => {
            matchLiteral([86, 97, 114], "Var", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("Identifier", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "PatternCore", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.10",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("c"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "PatternCore", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("args"),
              matchOptional(
                parseRule("CtorPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "PatternCore", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.11",
        matchLabeled(Some("c"),
          parseRule("LongConstructor", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("PatternCore:root.alternatives.12",
        matchLabeled(Some("v"),
          parseRule("LongName", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_ParamList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ParamList:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([40], "(", "ParamList", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("ps"),
          matchOptional(
            parseRule("ParamListItems", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "ParamList", source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_Param = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Param:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("pattern"),
          parseRule("ParamPattern", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("ann"),
          matchOptional(
            parseRule("TypeAnn", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};
