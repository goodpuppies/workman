-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: e229671a9f698fe9a83cde98c7e5757ce91ac2fd2484ca7881c86eacd0dc6b52
-- Directly compiled rules: ListPattern, ListPatternItem, ListLetPattern
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_ListPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ListPattern:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListPattern:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([46, 46], "..", "ListPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              parseRule("ListPatternItem", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListPattern:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("ListPatternItem", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchRepeated((source, offset) => {
                matchSyntaxAction("ListPattern:root.alternatives.2.expression.elements.3.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("p"),
                        parseRule("ListPatternItem", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              }, diagnose, source, offset)
            )
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              matchOptional(
                matchSyntaxAction("ListPattern:root.alternatives.2.expression.elements.4.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([46, 46], "..", "ListPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("t"),
                        parseRule("ListPatternItem", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchOptional(
              matchLiteral([44], ",", "ListPattern", source, offset)
            , diagnose, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_ListPatternItem = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ListPatternItem:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([86, 97, 114], "Var", "ListPatternItem", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "ListPatternItem", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("name"),
              parseRule("Identifier", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "ListPatternItem", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      parseRule("RecordPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ListPattern", source, offset, diagnose, recover)
    },
    (source, offset) => {
      matchSyntaxAction("ListPatternItem:root.alternatives.3",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("c"),
              parseRule("LongConstructor", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([40], "(", "ListPatternItem", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("args"),
              matchOptional(
                parseRule("CtorPatternList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([41], ")", "ListPatternItem", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListPatternItem:root.alternatives.4",
        matchLabeled(Some("c"),
          parseRule("LongConstructor", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListPatternItem:root.alternatives.5",
        matchLabeled(Some("v"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListPatternItem:root.alternatives.6",
        matchLabeled(Some("p"),
          parseRule("Pattern", source, offset, diagnose, recover)
        )
      )
    },
  ], diagnose, source, offset)
};

let rule_ListLetPattern = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ListLetPattern:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListLetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListLetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListLetPattern:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListLetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([46, 46], "..", "ListLetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListLetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ListLetPattern:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchLiteral([91], "[", "ListLetPattern", source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("first"),
              parseRule("LetPattern", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            matchLabeled(Some("rest"),
              matchRepeated((source, offset) => {
                matchSyntaxAction("ListLetPattern:root.alternatives.2.expression.elements.3.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListLetPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("p"),
                        parseRule("LetPattern", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              }, diagnose, source, offset)
            )
          },
          (source, offset) => {
            matchLabeled(Some("tail"),
              matchOptional(
                matchSyntaxAction("ListLetPattern:root.alternatives.2.expression.elements.4.expression.expression",
                  matchSequence([
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([44], ",", "ListLetPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLiteral([46, 46], "..", "ListLetPattern", source, offset)
                    },
                    (source, offset) => {
                      parseRule("_", source, offset, diagnose, false)
                    },
                    (source, offset) => {
                      matchLabeled(Some("t"),
                        parseRule("LetPattern", source, offset, diagnose, false)
                      )
                    },
                  ], diagnose, source, offset)
                )
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchOptional(
              matchLiteral([44], ",", "ListLetPattern", source, offset)
            , diagnose, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([93], "]", "ListLetPattern", source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};
