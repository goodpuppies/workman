-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: a5698108ba389db91ff03ab8795226ccc23344f605036261786d594354f4f248
-- Directly compiled rules: Start, TopPhrase, TopDecl, ImportDecl, ImportClause, ImportSpecList, ImportSpec, JsImportDecl
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_Start = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Start:root",
    matchSequence([
      (source, offset) => {
        matchSemanticPredicate("Start:root.expression.elements.0", "Start", offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("decls"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Start:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, recover)
                },
                (source, offset) => {
                  matchLabeled(Some("d"),
                    parseRule("TopPhrase", source, offset, diagnose, recover)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        parseRule("EOF", source, offset, diagnose, recover)
      },
    ], diagnose, source, offset)
  )
};

let rule_TopPhrase = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("TopPhrase:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("d"),
              parseRule("TopDecl", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableLiteral([59], ";", "TopPhrase:semicolon", recover, source, offset)
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("TopPhrase:root.alternatives.1",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("e"),
              parseRule("Expr", source, offset, diagnose, recover)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableLiteral([59], ";", "TopPhrase:semicolon", recover, source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_TopDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("JsImportDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("ImportDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("RecordDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("TypeDecl", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("LetDecl", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_ImportDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ImportDecl:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([102, 114, 111, 109], "from", "ImportDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("path"),
          parseRule("String", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([105, 109, 112, 111, 114, 116], "import", "ImportDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("clause"),
          parseRule("ImportClause", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_ImportClause = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("ImportClause:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLiteral([42], "*", "ImportClause", source, offset)
          },
          (source, offset) => {
            parseRule("__", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLiteral([97, 115], "as", "ImportClause", source, offset)
          },
          (source, offset) => {
            parseRule("__", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("alias"),
              parseRule("Name", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ImportClause:root.alternatives.1",
        matchLiteral([42], "*", "ImportClause", source, offset)
      )
    },
    (source, offset) => {
      matchSyntaxAction("ImportClause:root.alternatives.2",
        matchSequence([
          (source, offset) => {
            matchRecoverableOpeningBrace("ImportClause:open-brace", recover, source, offset)
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("specs"),
              matchOptional(
                parseRule("ImportSpecList", source, offset, diagnose, false)
              , diagnose, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchRecoverableLiteral([125], "}", "ImportClause:close-brace", recover, source, offset)
          },
        ], diagnose, source, offset)
      )
    },
  ], diagnose, source, offset)
};

let rule_ImportSpecList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ImportSpecList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("ImportSpec", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("ImportSpecList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "ImportSpecList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("s"),
                    parseRule("ImportSpec", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "ImportSpecList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_ImportSpec = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ImportSpec:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("Name", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("alias"),
          matchOptional(
            matchSyntaxAction("ImportSpec:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([97, 115], "as", "ImportSpec", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("a"),
                    parseRule("Identifier", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_JsImportDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("JsImportDecl:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([102, 114, 111, 109], "from", "JsImportDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("target"),
          parseRule("JsTarget", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([105, 109, 112, 111, 114, 116], "import", "JsImportDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("typeOnly"),
          matchOptional(
            matchSequence([
              (source, offset) => {
                matchLiteral([116, 121, 112, 101], "type", "JsImportDecl", source, offset)
              },
              (source, offset) => {
                parseRule("__", source, offset, diagnose, false)
              },
            ], diagnose, source, offset)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("clause"),
          parseRule("JsImportClause", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};
