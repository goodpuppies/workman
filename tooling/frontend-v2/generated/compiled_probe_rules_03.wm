-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: a5698108ba389db91ff03ab8795226ccc23344f605036261786d594354f4f248
-- Directly compiled rules: RecordFieldDeclList, RecordFieldDecl, TypeDecl, TypeDeclBody, Expr, IfExpr, MatchExpr, MatchFn, MatchFnParams
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_RecordFieldDeclList = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordFieldDeclList:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("RecordFieldDecl", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("RecordFieldDeclList:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "RecordFieldDeclList", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("f"),
                    parseRule("RecordFieldDecl", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "RecordFieldDeclList", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_RecordFieldDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("RecordFieldDecl:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("FieldName", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([58], ":", "RecordFieldDecl", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("type"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_TypeDecl = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("TypeDecl:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([116, 121, 112, 101], "type", "TypeDecl", source, offset)
      },
      (source, offset) => {
        parseRule("__", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("Constructor", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("params"),
          matchOptional(
            parseRule("TypeParams", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([61], "=", "TypeDecl", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("body"),
          parseRule("TypeDeclBody", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_TypeDeclBody = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("TypeDeclBody:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("hasLeadingPipe"),
          matchOptional(
            matchSyntaxAction("TypeDeclBody:root.expression.elements.0.expression.expression",
              matchSequence([
                (source, offset) => {
                  matchLiteral([124], "|", "TypeDeclBody", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("FnType", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("TypeDeclBody:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([124], "|", "TypeDeclBody", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("t"),
                    parseRule("FnType", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Expr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("IfExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("MatchFn", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("MatchExpr", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Lambda", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Or", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_IfExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("IfExpr:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([105, 102], "if", "IfExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([40], "(", "IfExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("cond"),
          parseRule("Expr", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "IfExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("thenExpr"),
          parseRule("Block", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([101, 108, 115, 101], "else", "IfExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("elseExpr"),
          parseRule("Block", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_MatchExpr = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MatchExpr:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([109, 97, 116, 99, 104], "match", "MatchExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([40], "(", "MatchExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("values"),
          parseRule("ArgList", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "MatchExpr", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("MatchExpr:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("arms"),
          parseRule("MatchArmList", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "MatchExpr:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_MatchFn = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MatchFn:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([109, 97, 116, 99, 104], "match", "MatchFn", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([40], "(", "MatchFn", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("names"),
          parseRule("MatchFnParams", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "MatchFn", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([61, 62], "=>", "MatchFn", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableOpeningBrace("MatchFn:open-brace", recover, source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("arms"),
          parseRule("MatchArmList", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchRecoverableLiteral([125], "}", "MatchFn:close-brace", recover, source, offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_MatchFnParams = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MatchFnParams:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Identifier", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("MatchFnParams:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([44], ",", "MatchFnParams", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("n"),
                    parseRule("Identifier", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchOptional(
          matchLiteral([44], ",", "MatchFnParams", source, offset)
        , diagnose, offset)
      },
    ], diagnose, source, offset)
  )
};
