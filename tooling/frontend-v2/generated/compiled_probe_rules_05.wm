-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: 7df5f1c7d683dc56d521679b882f50d1d68dec40fc7f5dc2c15fd44bab264994
-- Directly compiled rules: Equality, Compare, Add, Mul, Unary, Pipe, PipeMember, Postfix, ExplicitCall
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_Equality = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Equality:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Compare", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Equality:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchChoice([
                      (source, offset) => {
                        matchLiteral([61, 61], "==", "Equality", source, offset)
                      },
                      (source, offset) => {
                        matchLiteral([33, 61], "!=", "Equality", source, offset)
                      },
                    ], diagnose, source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Compare", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Compare = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Compare:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Add", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Compare:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchChoice([
                      (source, offset) => {
                        matchLiteral([60, 61], "<=", "Compare", source, offset)
                      },
                      (source, offset) => {
                        matchLiteral([62, 61], ">=", "Compare", source, offset)
                      },
                      (source, offset) => {
                        matchCharacterClass([SingleCode(60), SingleCode(62)], false, "a character in the class", "Compare", source, offset)
                      },
                    ], diagnose, source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Add", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Add = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Add:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Mul", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Add:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchChoice([
                      (source, offset) => {
                        matchLiteral([43, 43], "++", "Add", source, offset)
                      },
                      (source, offset) => {
                        matchCharacterClass([SingleCode(43), SingleCode(45)], false, "a character in the class", "Add", source, offset)
                      },
                    ], diagnose, source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Mul", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Mul = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Mul:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Unary", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Mul:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("op"),
                    matchCharacterClass([SingleCode(37), SingleCode(42), SingleCode(47)], false, "a character in the class", "Mul", source, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    parseRule("Unary", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Unary = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSyntaxAction("Unary:root.alternatives.0",
        matchSequence([
          (source, offset) => {
            matchLabeled(Some("op"),
              matchCharacterClass([SingleCode(33), SingleCode(45)], false, "a character in the class", "Unary", source, offset)
            )
          },
          (source, offset) => {
            parseRule("_", source, offset, diagnose, recover)
          },
          (source, offset) => {
            matchLabeled(Some("value"),
              parseRule("Unary", source, offset, diagnose, recover)
            )
          },
        ], diagnose, source, offset)
      )
    },
    (source, offset) => {
      parseRule("Pipe", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_Pipe = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Pipe:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("left"),
          parseRule("Postfix", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("Pipe:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([58, 62], ":>", "Pipe", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("right"),
                    matchChoice([
                      (source, offset) => {
                        parseRule("PipeMember", source, offset, diagnose, false)
                      },
                      (source, offset) => {
                        parseRule("Postfix", source, offset, diagnose, false)
                      },
                    ], diagnose, source, offset)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_PipeMember = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("PipeMember:root",
    matchSequence([
      (source, offset) => {
        matchLiteral([46], ".", "PipeMember", source, offset)
      },
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("MemberName", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("PipeMember:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  matchLiteral([46], ".", "PipeMember", source, offset)
                },
                (source, offset) => {
                  matchLabeled(Some("n"),
                    parseRule("MemberName", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("call"),
          matchOptional(
            matchSyntaxAction("PipeMember:root.expression.elements.3.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([40], "(", "PipeMember", source, offset)
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("args"),
                    matchOptional(
                      parseRule("ArgList", source, offset, diagnose, false)
                    , diagnose, offset)
                  )
                },
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLiteral([41], ")", "PipeMember", source, offset)
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Postfix = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Postfix:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("head"),
          parseRule("Primary", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("tail"),
          matchRepeated((source, offset) => {
            matchChoice([
              (source, offset) => {
                parseRule("ExplicitCall", source, offset, diagnose, false)
              },
              (source, offset) => {
                parseRule("SpaceCall", source, offset, diagnose, false)
              },
            ], diagnose, source, offset)
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchLabeled(Some("annotation"),
          matchOptional(
            matchSyntaxAction("Postfix:root.expression.elements.2.expression.expression",
              matchSequence([
                (source, offset) => {
                  parseRule("_", source, offset, diagnose, false)
                },
                (source, offset) => {
                  matchLabeled(Some("value"),
                    parseRule("TypeAnn", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          , diagnose, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_ExplicitCall = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("ExplicitCall:root",
    matchSequence([
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([40], "(", "ExplicitCall", source, offset)
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLabeled(Some("args"),
          matchOptional(
            parseRule("ArgList", source, offset, diagnose, false)
          , diagnose, offset)
        )
      },
      (source, offset) => {
        parseRule("_", source, offset, diagnose, recover)
      },
      (source, offset) => {
        matchLiteral([41], ")", "ExplicitCall", source, offset)
      },
      (source, offset) => {
        matchNegativeLookahead(
          matchSequence([
            (source, offset) => {
              parseRule("_", source, offset, diagnose, false)
            },
            (source, offset) => {
              matchLiteral([61, 62], "=>", "ExplicitCall", source, offset)
            },
          ], diagnose, source, offset)
        , offset)
      },
    ], diagnose, source, offset)
  )
};
