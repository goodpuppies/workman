-- Generated by scripts/generate_frontend_v2_recognizer.ts. Do not edit.
-- Grammar: src/grammar.peggy
-- Grammar IR SHA-256: e229671a9f698fe9a83cde98c7e5757ce91ac2fd2484ca7881c86eacd0dc6b52
-- Directly compiled rules: Wildcard, Identifier, Constructor, Name, MemberName, FieldName, LongName, LongConstructor, IdentPart, Keyword, __, _, Whitespace, Comment, EOF
from "../compiled_probe_runtime.wm" import { CompiledProbeMatch, matchLiteral, matchRecoverableLiteral, matchRecoverableOpeningBrace, matchCharacterClass, matchAnyCharacter, matchSequence, matchChoice, matchOptional, matchRepeated, matchOneOrMore, matchNegativeLookahead, matchText, matchLabeled, matchSyntaxAction, matchSemanticPredicate };
from "./compiled_probe_types.wm" import { SingleCode, CodeRange };

let rule_Wildcard = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSequence([
    (source, offset) => {
      matchLiteral([95], "_", "Wildcard", source, offset)
    },
    (source, offset) => {
      matchNegativeLookahead(
        parseRule("IdentPart", source, offset, diagnose, false)
      , offset)
    },
  ], diagnose, source, offset)
};

let rule_Identifier = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Identifier:root",
    matchSequence([
      (source, offset) => {
        matchNegativeLookahead(
          parseRule("Keyword", source, offset, diagnose, false)
        , offset)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          matchText(
            matchSequence([
              (source, offset) => {
                matchCharacterClass([CodeRange(97, 122), SingleCode(95)], false, "a character in the class", "Identifier", source, offset)
              },
              (source, offset) => {
                matchRepeated((source, offset) => {
                  parseRule("IdentPart", source, offset, diagnose, false)
                }, diagnose, source, offset)
              },
            ], diagnose, source, offset)
          , offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Constructor = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("Constructor:root",
    matchSequence([
      (source, offset) => {
        matchNegativeLookahead(
          parseRule("Keyword", source, offset, diagnose, false)
        , offset)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          matchText(
            matchSequence([
              (source, offset) => {
                matchCharacterClass([CodeRange(65, 90)], false, "a character in the class", "Constructor", source, offset)
              },
              (source, offset) => {
                matchRepeated((source, offset) => {
                  parseRule("IdentPart", source, offset, diagnose, false)
                }, diagnose, source, offset)
              },
            ], diagnose, source, offset)
          , offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_Name = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      parseRule("Constructor", source, offset, diagnose, recover)
    },
    (source, offset) => {
      parseRule("Identifier", source, offset, diagnose, recover)
    },
  ], diagnose, source, offset)
};

let rule_MemberName = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("MemberName:root",
    matchLabeled(Some("name"),
      matchText(
        matchSequence([
          (source, offset) => {
            matchCharacterClass([CodeRange(97, 122), CodeRange(65, 90), SingleCode(95)], false, "a character in the class", "MemberName", source, offset)
          },
          (source, offset) => {
            matchRepeated((source, offset) => {
              parseRule("IdentPart", source, offset, diagnose, false)
            }, diagnose, source, offset)
          },
        ], diagnose, source, offset)
      , offset)
    )
  )
};

let rule_FieldName = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("FieldName:root",
    matchSequence([
      (source, offset) => {
        matchNegativeLookahead(
          parseRule("Keyword", source, offset, diagnose, false)
        , offset)
      },
      (source, offset) => {
        matchLabeled(Some("name"),
          parseRule("MemberName", source, offset, diagnose, recover)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_LongName = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LongName:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Name", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LongName:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  matchLiteral([46], ".", "LongName", source, offset)
                },
                (source, offset) => {
                  matchLabeled(Some("n"),
                    parseRule("MemberName", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
    ], diagnose, source, offset)
  )
};

let rule_LongConstructor = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSyntaxAction("LongConstructor:root",
    matchSequence([
      (source, offset) => {
        matchLabeled(Some("first"),
          parseRule("Constructor", source, offset, diagnose, recover)
        )
      },
      (source, offset) => {
        matchLabeled(Some("rest"),
          matchRepeated((source, offset) => {
            matchSyntaxAction("LongConstructor:root.expression.elements.1.expression.expression",
              matchSequence([
                (source, offset) => {
                  matchLiteral([46], ".", "LongConstructor", source, offset)
                },
                (source, offset) => {
                  matchLabeled(Some("n"),
                    parseRule("Constructor", source, offset, diagnose, false)
                  )
                },
              ], diagnose, source, offset)
            )
          }, diagnose, source, offset)
        )
      },
      (source, offset) => {
        matchNegativeLookahead(
          matchLiteral([46], ".", "LongConstructor", source, offset)
        , offset)
      },
    ], diagnose, source, offset)
  )
};

let rule_IdentPart = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchCharacterClass([CodeRange(97, 122), CodeRange(65, 90), CodeRange(48, 57), SingleCode(95)], false, "a character in the class", "IdentPart", source, offset)
};

let rule_Keyword = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchSequence([
    (source, offset) => {
      matchChoice([
        (source, offset) => {
          matchLiteral([102, 114, 111, 109], "from", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([105, 109, 112, 111, 114, 116], "import", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([97, 115], "as", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([108, 101, 116], "let", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([114, 101, 99], "rec", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([97, 110, 100], "and", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([116, 121, 112, 101], "type", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([114, 101, 99, 111, 114, 100], "record", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([105, 102], "if", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([101, 108, 115, 101], "else", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([109, 97, 116, 99, 104], "match", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([116, 114, 117, 101], "true", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([102, 97, 108, 115, 101], "false", "Keyword", source, offset)
        },
        (source, offset) => {
          matchLiteral([118, 111, 105, 100], "void", "Keyword", source, offset)
        },
      ], diagnose, source, offset)
    },
    (source, offset) => {
      matchNegativeLookahead(
        parseRule("IdentPart", source, offset, diagnose, false)
      , offset)
    },
  ], diagnose, source, offset)
};

let rule___ = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchOneOrMore((source, offset) => {
    matchChoice([
      (source, offset) => {
        parseRule("Whitespace", source, offset, diagnose, false)
      },
      (source, offset) => {
        parseRule("Comment", source, offset, diagnose, false)
      },
    ], diagnose, source, offset)
  }, diagnose, source, offset)
};

let rule__ = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchRepeated((source, offset) => {
    matchChoice([
      (source, offset) => {
        parseRule("Whitespace", source, offset, diagnose, false)
      },
      (source, offset) => {
        parseRule("Comment", source, offset, diagnose, false)
      },
    ], diagnose, source, offset)
  }, diagnose, source, offset)
};

let rule_Whitespace = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchCharacterClass([SingleCode(32), SingleCode(9), SingleCode(13), SingleCode(10)], false, "a character in the class", "Whitespace", source, offset)
};

let rule_Comment = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchChoice([
    (source, offset) => {
      matchSequence([
        (source, offset) => {
          matchLiteral([45, 45], "--", "Comment", source, offset)
        },
        (source, offset) => {
          matchRepeated((source, offset) => {
            matchCharacterClass([SingleCode(10)], true, "a character outside the class", "Comment", source, offset)
          }, diagnose, source, offset)
        },
      ], diagnose, source, offset)
    },
    (source, offset) => {
      matchSequence([
        (source, offset) => {
          matchLiteral([47, 47], "//", "Comment", source, offset)
        },
        (source, offset) => {
          matchRepeated((source, offset) => {
            matchCharacterClass([SingleCode(10)], true, "a character outside the class", "Comment", source, offset)
          }, diagnose, source, offset)
        },
      ], diagnose, source, offset)
    },
  ], diagnose, source, offset)
};

let rule_EOF = (source: String, offset: Number, recover: Bool, diagnose: Bool, parseRule): CompiledProbeMatch => {
  matchNegativeLookahead(
    matchAnyCharacter("EOF", source, offset)
  , offset)
};
