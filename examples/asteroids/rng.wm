record Seed = { value: Number };

let next = (r: Seed) => {
  Seed((r.value * 16807) % 2147483647)
};

let unit = (r: Seed) => {
  r.value / 2147483647
};

let between = (r: Seed, lo: Number, hi: Number) => {
  lo + unit(r) * (hi - lo)
};

let flip = (r: Seed) => {
  unit(r) < 0.5
};
